# API base URL — used by Vite dev-server proxy and for direct fetch calls
VITE_API_URL=http://localhost:3001

# WebSocket URL — falls back to VITE_API_URL if not set
VITE_WS_URL=http://localhost:3001

# Default project ID loaded in the UI
VITE_PROJECT_ID=your-project-uuid-here
